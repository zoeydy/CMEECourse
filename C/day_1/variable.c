# include <stdio.h>

int main(void)
{
    // declare the variables
    int x = 10;
    int y = 5;
    int z = 1.1;
    // char* a = "a";
    char a[] = "a";

    // print the variables to the console
    printf("The value of variable x is %i\n", x);
    printf("The value of variable y is %i\n", y);
    printf("The value of variable z is %i\n", z);
    printf("The value of variable a is %s\n", a);

    return 0;
}