#include <stdio.h>

int main(void)
{
    int x = 1;
    char a[] = "a";
    float point = 1.1;

    printf("the value of integer x is %i\n", x);
    printf("the value of charactor a is %s\n", a);
    printf("the value of flout point is %f\n", point);

    return 0;
}