# include <stdio.h>

int main(void)
{
    int x;
    char c;
    float f;
    double d;

    printf("the integer x is: %i,\n", x);
    printf("the charactor char is: %c\n", c);
    printf("the float is: %f\n", f);
    printf("the double is: %e\n", d);

    return 0;
}